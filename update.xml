<?xml version="1.0" encoding="UTF-8"?>
<updateInfo>
    <!-- Basic version information -->
    <version>1.2.0</version>
    <releaseDate>2025-03-31</releaseDate>
    
    <!-- Release notes (supports HTML formatting) -->
    <description>
        <![CDATA[
        This update includes:
        • Automatic update system with in-app downloads
        • Improved performance when loading large datasets
        • Fixed currency conversion issues
        • Added support for additional file formats
        • Better error handling
        ]]>
    </description>
    
    <downloadUrl>https://raw.githubusercontent.com/dbo9h/FPCLA/main/FilamentProfitCalc.zip</downloadUrl>
    
    <!-- Filename to save the download as (MUST match your actual file's name) -->
    <fileName>Profi.zip</fileName>
    
    <!-- Optional: Command-line arguments for installer files (can be empty) -->
    <launchArgs></launchArgs>
    
    <!-- Whether the update requires administrator privileges -->
    <requiresAdmin>false</requiresAdmin>
    
    <!-- Whether the update is mandatory (if true, users can't skip) -->
    <mandatory>false</mandatory>
    
    <!-- Minimum required version - users below this must update -->
    <minimumRequiredVersion>1.0.0</minimumRequiredVersion>
</updateInfo>
